# File Purge Configuration

# Path to the directory you want to monitor (on your host system) or Docker volume name
# This will be mounted as a volume in the Docker container
PURGE_VOLUME=/path/to/your/data # or docker volume name

# Maximum age of files in days (files older than this will be deleted)
PURGE_MAX_AGE_DAYS=30

# Check interval in seconds (how often to scan for old files)
# 3600 = 1 hour, 86400 = 24 hours
PURGE_CHECK_INTERVAL=3600

# Discord Webhook URL (get this from your Discord server settings)
# Server Settings -> Integrations -> Webhooks -> New Webhook
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Dry run mode - set to true to test without actually deleting files
# Set to false for production use
PURGE_DRY_RUN=true
